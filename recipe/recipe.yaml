context:
  version: "0.17.1"
package:
  name: mdbook-mermaid
  version: ${{ version }}
source:
  url: https://github.com/badboy/mdbook-mermaid/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 5b0242092325a383c46ad9fcd15f2f6972ccc549b640fcb74bc02cf3d2cf45d4
build:
  number: 0
  script:
    env:
      CARGO_PROFILE_RELEASE_STRIP: symbols
      CARGO_PROFILE_RELEASE_LTO: fat
    content:
      - if: unix
        then: |
          cargo-bundle-licenses --format yaml --output ${SRC_DIR}/THIRDPARTY.yml
          cargo auditable install --locked --no-track --path . --root ${PREFIX}
        else: |
          cargo-bundle-licenses --format yaml --output %SRC_DIR%\THIRDPARTY.yml
          cargo auditable install --locked --no-track --path . --root %LIBRARY_PREFIX%
requirements:
  build:
    - ${{ compiler('rust') }}
    - if: linux
      then: ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - cargo-bundle-licenses
    - cargo-auditable
tests:
  - script:
      - mdbook-mermaid --version
about:
  homepage: https://github.com/badboy/mdbook-mermaid
  license: MPL-2.0
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  description: A preprocessor for mdbook to add mermaid support
  summary: A preprocessor for mdbook to add mermaid support
extra:
  recipe-maintainers:
    - VasanthakumarV
